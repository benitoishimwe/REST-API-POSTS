# MongoDB connection string
DB_CONNECTION=mongodb://localhost:27017/rest_api_posts

# Port the server listens on (default: 4000)
PORT=4000

# Secret for signing JWT tokens (planned — not used yet)
JWT_SECRET=your_jwt_secret_here
